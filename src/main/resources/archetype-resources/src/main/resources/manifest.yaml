name: ${Name}
label: ${Name}
description: ${Description}
version: ${Version}
type: ${Type}
author: ${Author}
credentials:
  key:
    help:
    label:
    placeholder:
    required: true
    type: secret-input
    url:
meta:
  runner:
    type: java
    class: ${package}.${Name}Plugin
tools:
  - name:
    label:
    description:
    meta:
      runner:
        type: java
        class: ${package}.tools
    parameters:
      - name: query
        description:
        label:
        required: true
        type: text
